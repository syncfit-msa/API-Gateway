spring:
  config:
    import: optional:configserver:http://127.0.0.1:8888/
  cloud:
    config:
      name: api-gateway
  application:
    name: api-gateway
  profiles:
    active: local
    include:
      - jwt
      - eureka
      - actuator
